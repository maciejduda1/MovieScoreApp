doctype html
html
    head
        title="App Page"
    body
        h1.app-title  Hello! #{name}. Tell us what movies you like!!
        block add
            form(action="/addmovie", method="post")
                input(
                    type = "text",
                    name = "movie_title",
                    value = "",
                    placeholder = "movie title"
                )
                input(
                    type = "number",
                    value = "",
                    name = "movie_score",
                    placeholder = "movie score "
                )
                input(
                    type = "submit",
                    value = "Submit"
                )
        block table
        table
            thead
                tr
                    th Film
                    th Ocena
                    th Delete
            tbody
                - var contentTable = movies;
                - console.log('pug ' + movies.length);
                - function Delete(item) {
                -    console.log('item  ' + item);
                -    deleteMovie(name, item);
                -    return console.log('pug item ' + item); 
                - //document.getElementById(item).deleteRow(item);
                - }
                each item, index in contentTable
                    tr#index
                        td= item.title
                        td= item.score
                        td 
                            button( type= "button", onclick= Delete(this.item)) Delete
